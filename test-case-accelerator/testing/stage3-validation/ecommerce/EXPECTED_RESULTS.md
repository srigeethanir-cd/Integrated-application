# Expected Stage 3 Results

- Three `/api/v1/store` endpoints with `get_db`.
- Models `Product`, `Order`, `OrderItem`; two foreign keys and bidirectional relationships.
- Product constraints, inventory business rule, discount rule, commit and rollback.
- HTTP 404 and 409 mappings on checkout.
