# Banking API

FastAPI banking fixture covering authenticated accounts, deposits, transfers,
balance rules, SQLAlchemy transactions, commits, refreshes, and rollbacks.

Run with `uvicorn main:app --reload`; run tests with `pytest`.
