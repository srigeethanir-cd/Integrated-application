# TestForge enterprise validation fixture

This deliberately compact FastAPI application exercises deterministic discovery,
semantic inference, unit-test generation, verification, runtime validation, and
export across layered CRUD, authentication, validation, exceptions, and SQLite.
