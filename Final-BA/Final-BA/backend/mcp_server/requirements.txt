# MCP Server — Python dependencies
# Install with:  pip install -r backend/mcp_server/requirements.txt

# Jira Cloud client
jira>=3.5.0

# Confluence / Atlassian client
atlassian-python-api>=3.41.0

# Google Drive OAuth2 & API
google-api-python-client>=2.90.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0

# PDF text extraction
PyMuPDF>=1.23.0

# DOCX text extraction
python-docx>=0.8.11

# HTML → plain text
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Env vars
python-dotenv>=1.0.0

# Pydantic (likely already in backend deps, but pinned for clarity)
pydantic>=2.0.0

# FastAPI + ASGI server (for standalone Swagger UI mode)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
